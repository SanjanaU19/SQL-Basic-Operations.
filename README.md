# SQL-Basic-Operations.
Hospital Management System – MySQL Project: A database project that manages departments, doctors, patients, appointments, bills, and payments using MySQL. It demonstrates database concepts such as tables, primary keys, foreign keys, CRUD operations, filtering, sorting, and basic SQL queries.
